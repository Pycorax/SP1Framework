#ifndef CUSTOM_LEVELS_H
#define CUSTOM_LEVELS_H

#include <vector>
#include <string>

using std::vector;
using std::string;

int findCustomMaps(vector<string> &fileNames);
int findCustomMaps();

#endif